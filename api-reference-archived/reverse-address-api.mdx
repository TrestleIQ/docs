---
title: "Reverse Address API (Archived)"
description: "Reverse Address API validates and normalizes the input address and provides the current residents at the address with their information, including demographics, historical addresses, relatives and associated people and phone numbers."
api: "GET /3.0/location"
---

<Warning>
  This is an archived version of the API. Please use the [Current Reverse
  Address API](/api-reference/reverse-address-api) for new integrations.
</Warning>

<RequestExample>

```bash
GET https://api.trestleiq.com/3.0/location?address.street_line_1={street}&address.city={city}&address.state_code={state}&address.postal_code={postal_code}
```

</RequestExample>

## Parameters

<ParamField query="street_line_1" type="string">
  The first line of the street part in the structured address. Cannot be longer
  than 1000 characters. **Example:** `100 Main St`
</ParamField>

<ParamField query="street_line_2" type="string">
  The second line of the street part in the structured address. Cannot be longer
  than 1000 characters. **Example:** `Ste 1`
</ParamField>

<ParamField query="city" type="string">
  The name of the city in the structured address. Cannot be longer than 500
  characters. **Example:** `Seattle`
</ParamField>

<ParamField query="state_code" type="string">
  The state code of the structured address. Cannot be longer than 100
  characters. **Example:** `WA`
</ParamField>

<ParamField query="postal_code" type="string">
  The postal code of the structured address. Cannot be longer than 100
  characters. **Example:** `98101`
</ParamField>

<ParamField query="country_code" type="string">
  The ISO-3166 alpha-2 country code of the address.
  See:[ISO-3166](https://www.nationsonline.org/oneworld/country_code_list.htm).
  Cannot be longer than 100 characters. **Example:** `US`
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key for authentication.
</ParamField>

## Code Examples

<CodeGroup>

```bash cURL
curl --request GET \
  --url "https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101" \
  --header "x-api-key: YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch(
  "https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101",
  {
    headers: {
      "x-api-key": "YOUR_API_KEY",
    },
  }
);
const data = await response.json();
```

```javascript Node.js
import axios from "axios";

const { data } = await axios.get(
  "https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101",
  {
    headers: {
      "x-api-key": "YOUR_API_KEY",
    },
  }
);
console.log(data);
```

```python Python
import requests

headers = {"x-api-key": "YOUR_API_KEY"}
params = {
    "address.street_line_1": "100 Main St",
    "address.city": "Seattle",
    "address.state_code": "WA",
    "address.postal_code": "98101",
}

response = requests.get(
    "https://api.trestleiq.com/3.0/location",
    params=params,
    headers=headers,
    timeout=30,
)
data = response.json()
print(data)
```

```csharp C#
using var client = new HttpClient();
client.DefaultRequestHeaders.Add("x-api-key", "YOUR_API_KEY");

var response = await client.GetAsync("https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101");
var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

```go Go
package main

import (
	"fmt"
	"io"
	"net/http"
)

func main() {
	req, _ := http.NewRequest("GET", "https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101", nil)
	req.Header.Set("x-api-key", "YOUR_API_KEY")

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()

	body, _ := io.ReadAll(resp.Body)
	fmt.Println(string(body))
}
```

```php PHP
<?php
$curl = curl_init("https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101");
curl_setopt($curl, CURLOPT_HTTPHEADER, ["x-api-key: YOUR_API_KEY"]);
curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($curl);
curl_close($curl);

echo $response;
```

```java Java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class ReverseAddressArchivedExample {
  public static void main(String[] args) throws Exception {
    HttpRequest request =
        HttpRequest.newBuilder()
            .uri(
                URI.create(
                    "https://api.trestleiq.com/3.0/location?address.street_line_1=100%20Main%20St&address.city=Seattle&address.state_code=WA&address.postal_code=98101"))
            .header("x-api-key", "YOUR_API_KEY")
            .GET()
            .build();

    HttpResponse<String> response =
        HttpClient.newHttpClient().send(request, HttpResponse.BodyHandlers.ofString());
    System.out.println(response.body());
  }
}
```

</CodeGroup>

**Postman payload**

```json
{
  "method": "GET",
  "url": "https://api.trestleiq.com/3.0/location",
  "query": {
    "address.street_line_1": "100 Main St",
    "address.city": "Seattle",
    "address.state_code": "WA",
    "address.postal_code": "98101"
  },
  "headers": { "x-api-key": "YOUR_API_KEY" }
}
```

## Response

<ResponseField name="id" type="string">
  The persistent ID of the address. Format: `Location.<uuid>`
</ResponseField>

<ResponseField name="is_valid" type="boolean">
  True if the address is valid.
</ResponseField>

<ResponseField name="street_line_1" type="string">
  The first line of the street part in the structured address.
</ResponseField>

<ResponseField name="street_line_2" type="string">
  The second line of the street part in the structured address.
</ResponseField>

<ResponseField name="city" type="string">
  The name of the city in the structured address.
</ResponseField>

<ResponseField name="postal_code" type="string">
  The postal code of the structured address.
</ResponseField>

<ResponseField name="zip4" type="string">
  The ZIP+4 code of the structured address (USA).
</ResponseField>

<ResponseField name="state_code" type="string">
  The state code of the structured address.
</ResponseField>

<ResponseField name="country_code" type="string">
  The ISO-3166 alpha-2 country code of the address.
</ResponseField>

<ResponseField name="lat_long" type="object">
  The coordinates of the geographical location of the address.
  <Expandable title="lat_long object">

    <ResponseField name="latitude" type="number">
      The latitude coordinate of the location.
    </ResponseField>

    <ResponseField name="longitude" type="number">
      The longer coordinate of the location.
    </ResponseField>

    <ResponseField name="accuracy" type="string">
      The accuracy of the geographical location. Enum: "Neighborhood","Country","PostalCode","Street","City","State","RoofTop"
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="is_active" type="boolean">
  True if the address is currently receiving mail.
</ResponseField>

<ResponseField name="is_commercial" type="array">
  True if the address is a business address. When shipping physical goods,
  commercial addresses are typically a positive signal and indicate that the
  order is not fraudulent.
</ResponseField>

<ResponseField name="is_forwarder" type="boolean">
  True if the address is performing freight forwarding or reshipping services.
  Forwarders most likely will send the goods outside of the country.
</ResponseField>

<ResponseField name="delivery_point" type="array">
  The type of the delivery point of the address. Enum: "SingleUnit",
  "MultiUnit", "POBox", "PartialAddress"
</ResponseField>

<ResponseField name="current_residents" type="object">
  The list of current person and business residents at the address.
  <Expandable title="owner object">
    <ResponseField name="id" type="string">
      The persistent ID of the address.
    </ResponseField>

    <ResponseField name="name" type="string">
      The full name of the person.
    </ResponseField>

    <ResponseField name="firstname" type="string">
      The first name of the person.
    </ResponseField>

    <ResponseField name="middlename" type="string">
      The middle name (or middle initial) of the person.
    </ResponseField>

    <ResponseField name="lastname" type="string">
      The last name of the person.
    </ResponseField>

    <ResponseField name="alternate_names" type="string">
      Alternate names of the person associated with the phone.
    </ResponseField>

    <ResponseField name="age_range" type="string">
      The age of the person in a 5-year range.
    </ResponseField>

    <ResponseField name="gender" type="string">
      The gender of the person.
    </ResponseField>

    <ResponseField name="type" type="string">
      The type of the legal entity.
        - Person: The legal entity is a person.
        - Company: The legal entity is a company.
    </ResponseField>

    <ResponseField name="link_to_phone_start_date" type="string">
      The date when the person was first linked to the phone number.
    </ResponseField>

    <ResponseField name="industry" type="array">
      Not applicable.
    </ResponseField>

    <ResponseField name="phones" type="array">
      The phone numbers associated with the person.
      <Expandable title="alternate_phone object">
        <ResponseField name="id" type="string">
          The persistent ID of the phone number.
        </ResponseField>
        <ResponseField name="phone_number" type="string">
          The phone number in E.164 or local format. The default country calling code is +1 (USA).
        </ResponseField>

        <ResponseField name="line_type" type="string">
          The line type of the phone number. Possible values: - `Landline` - Traditional
          wired phone line - `Mobile` - Wireless phone line - `FixedVOIP` - VOIP number
          connected to a physical address - `NonFixedVOIP` - VOIP number unconnected to
          a fixed physical address - `Premium` - Caller pays a premium for the call -
          `TollFree` - Callee pays for call - `Voicemail` - Voicemail-only service -
          `Other` - Line type is unclear
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="emails" type="string[]">
      The list of emails associated to the primary owner of the phone.
    </ResponseField>

    <ResponseField name="historical_addresses" type="object">
      A list of unique current locations associated with Person or Business in belongs_to array.
      <Expandable title="address object">
        <ResponseField name="id" type="string">
          The persistent ID of the address.
        </ResponseField>

        <ResponseField name="location_type" type="string">
          The type of the location. Enum: "Address","Neighborhood","Country","ZipPlus4","PostalCode","City","State"
        </ResponseField>

        <ResponseField name="street_line_1" type="string">
          The first line of the street part in the structured address.
        </ResponseField>

        <ResponseField name="street_line_2" type="string">
          The second line of the street part in the structured address.
        </ResponseField>

        <ResponseField name="city" type="string">
          The city associated with the address.
        </ResponseField>

        <ResponseField name="postal_code" type="string">
          The postal code of the structured address.
        </ResponseField>

        <ResponseField name="zip4" type="string">
          The ZIP+4 code of the structured address (USA).
        </ResponseField>

        <ResponseField name="state_code" type="string">
          The state code of the structured address.
        </ResponseField>

        <ResponseField name="country_code" type="string">
          The ISO-3166 alpha-2 country code of the phone number. See:[ISO-3166](https://www.nationsonline.org/oneworld/country_code_list.htm).
        </ResponseField>

        <ResponseField name="lat_long" type="object">
          The coordinates of the geographical location of the address.
          <Expandable title="lat_long object">
            <ResponseField name="latitude" type="number">
              The latitude coordinate of the location.
            </ResponseField>

            <ResponseField name="longitude" type="number">
              The longer coordinate of the location.
            </ResponseField>

            <ResponseField name="accuracy" type="string">
              The accuracy of the geographical location. Enum: "Neighborhood","Country","PostalCode","Street","City","State","RoofTop"
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="is_active" type="boolean">
          True if the address is currently receiving mail.
        </ResponseField>

        <ResponseField name="delivery_point" type="string">
          The type of the delivery point of the address. Enum: "SingleUnit","MultiUnit","POBox","PartialAddress"
        </ResponseField>

        <ResponseField name="link_to_person_start_date" type="string">
          The date when the address was first linked to the person.
        </ResponseField>

        <ResponseField name="link_to_person_end_date" type="string">
          The date when the address was first linked to the person.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="associated_people" type="object">
      The people that are associated to the person.
      <Expandable title="belongs_to object">
      <ResponseField name="id" type="string">
          The persistent ID of the address.
        </ResponseField>

        <ResponseField name="name" type="string">
          The full name of the person.
        </ResponseField>

        <ResponseField name="firstname" type="string">
          The first name of the person.
        </ResponseField>

        <ResponseField name="middlename" type="string">
          The middle name (or middle initial) of the person.
        </ResponseField>

        <ResponseField name="lastname" type="string">
          The last name of the person.
        </ResponseField>

      </Expandable>
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="last_sale_date" type="string">
  The date of the last sale of the property. Available for US addresses only.
</ResponseField>

<ResponseField name="total_value" type="array">
  The total assessed tax value of the property in USD. Available for US
  addresses only.
</ResponseField>

<ResponseField name="owners" type="object">
  The list of the current owners of the property. Available for US addresses only.
  <Expandable title="owner object">
    <ResponseField name="id" type="string">
      The persistent ID of the address.
    </ResponseField>

    <ResponseField name="name" type="string">
      The full name of the person.
    </ResponseField>

    <ResponseField name="firstname" type="string">
      The first name of the person.
    </ResponseField>

    <ResponseField name="middlename" type="string">
      The middle name (or middle initial) of the person.
    </ResponseField>

    <ResponseField name="lastname" type="string">
      The last name of the person.
    </ResponseField>

    <ResponseField name="alternate_names" type="string">
      Alternate names of the person associated with the phone.
    </ResponseField>

    <ResponseField name="age_range" type="string">
      The age of the person in a 5-year range.
    </ResponseField>

    <ResponseField name="gender" type="string">
      The gender of the person.
    </ResponseField>

    <ResponseField name="type" type="string">
      The type of the legal entity.
        - Person: The legal entity is a person.
        - Company: The legal entity is a company.
    </ResponseField>

    <ResponseField name="link_to_phone_start_date" type="string">
      The date when the person was first linked to the phone number.
    </ResponseField>

    <ResponseField name="industry" type="array">
      Not applicable.
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="associated_addresses" type="boolean">
  The list of addresses associated to the address.
  <Expandable title="associated_addresses object">
    <ResponseField name="id" type="string">
      The persistent ID of the address. Format: `Location.<uuid>`
    </ResponseField>

    <ResponseField name="is_valid" type="boolean">
      True if the address is valid.
    </ResponseField>

    <ResponseField name="street_line_1" type="string">
      The first line of the street part in the structured address.
    </ResponseField>

    <ResponseField name="street_line_2" type="string">
      The second line of the street part in the structured address.
    </ResponseField>

    <ResponseField name="city" type="string">
      The name of the city in the structured address.
    </ResponseField>

    <ResponseField name="postal_code" type="string">
      The postal code of the structured address.
    </ResponseField>

    <ResponseField name="zip4" type="string">
      The ZIP+4 code of the structured address (USA).
    </ResponseField>

    <ResponseField name="state_code" type="string">
      The state code of the structured address.
    </ResponseField>

    <ResponseField name="country_code" type="string">
      The ISO-3166 alpha-2 country code of the address.
    </ResponseField>

    <ResponseField name="lat_long" type="object">
      The coordinates of the geographical location of the address.
      <Expandable title="lat_long object">

        <ResponseField name="latitude" type="number">
          The latitude coordinate of the location.
        </ResponseField>

        <ResponseField name="longitude" type="number">
          The longer coordinate of the location.
        </ResponseField>

        <ResponseField name="accuracy" type="string">
          The accuracy of the geographical location. Enum: "Neighborhood","Country","PostalCode","Street","City","State","RoofTop"
        </ResponseField>

      </Expandable>
    </ResponseField>

    <ResponseField name="is_active" type="boolean">
      True if the address is currently receiving mail.
    </ResponseField>

    <ResponseField name="is_commercial" type="array">
      True if the address is a business address. When shipping physical goods,
      commercial addresses are typically a positive signal and indicate that the
      order is not fraudulent.
    </ResponseField>

    <ResponseField name="is_forwarder" type="boolean">
      True if the address is performing freight forwarding or reshipping services.
      Forwarders most likely will send the goods outside of the country.
    </ResponseField>

    <ResponseField name="delivery_point" type="array">
      The type of the delivery point of the address. Enum: "SingleUnit",
      "MultiUnit", "POBox", "PartialAddress"
    </ResponseField>

    <ResponseField name="current_residents" type="object">
      The list of current person and business residents at the address.
      <Expandable title="owner object">
        <ResponseField name="id" type="string">
          The persistent ID of the address.
        </ResponseField>

        <ResponseField name="name" type="string">
          The full name of the person.
        </ResponseField>

        <ResponseField name="firstname" type="string">
          The first name of the person.
        </ResponseField>

        <ResponseField name="middlename" type="string">
          The middle name (or middle initial) of the person.
        </ResponseField>

        <ResponseField name="lastname" type="string">
          The last name of the person.
        </ResponseField>

        <ResponseField name="alternate_names" type="string">
          Alternate names of the person associated with the phone.
        </ResponseField>

        <ResponseField name="age_range" type="string">
          The age of the person in a 5-year range.
        </ResponseField>

        <ResponseField name="gender" type="string">
          The gender of the person.
        </ResponseField>

        <ResponseField name="type" type="string">
          The type of the legal entity.
            - Person: The legal entity is a person.
            - Company: The legal entity is a company.
        </ResponseField>

        <ResponseField name="link_to_phone_start_date" type="string">
          The date when the person was first linked to the phone number.
        </ResponseField>

        <ResponseField name="industry" type="array">
          Not applicable.
        </ResponseField>

        <ResponseField name="phones" type="array">
          The phone numbers associated with the person.
          <Expandable title="alternate_phone object">
            <ResponseField name="id" type="string">
              The persistent ID of the phone number.
            </ResponseField>
            <ResponseField name="phone_number" type="string">
              The phone number in E.164 or local format. The default country calling code is +1 (USA).
            </ResponseField>

            <ResponseField name="line_type" type="string">
              The line type of the phone number. Possible values: - `Landline` - Traditional
              wired phone line - `Mobile` - Wireless phone line - `FixedVOIP` - VOIP number
              connected to a physical address - `NonFixedVOIP` - VOIP number unconnected to
              a fixed physical address - `Premium` - Caller pays a premium for the call -
              `TollFree` - Callee pays for call - `Voicemail` - Voicemail-only service -
              `Other` - Line type is unclear
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="historical_addresses" type="object">
          A list of unique current locations associated with Person or Business in belongs_to array.
          <Expandable title="address object">
            <ResponseField name="id" type="string">
              The persistent ID of the address.
            </ResponseField>

            <ResponseField name="location_type" type="string">
              The type of the location. Enum: "Address","Neighborhood","Country","ZipPlus4","PostalCode","City","State"
            </ResponseField>

            <ResponseField name="street_line_1" type="string">
              The first line of the street part in the structured address.
            </ResponseField>

            <ResponseField name="street_line_2" type="string">
              The second line of the street part in the structured address.
            </ResponseField>

            <ResponseField name="city" type="string">
              The city associated with the address.
            </ResponseField>

            <ResponseField name="postal_code" type="string">
              The postal code of the structured address.
            </ResponseField>

            <ResponseField name="zip4" type="string">
              The ZIP+4 code of the structured address (USA).
            </ResponseField>

            <ResponseField name="state_code" type="string">
              The state code of the structured address.
            </ResponseField>

            <ResponseField name="country_code" type="string">
              The ISO-3166 alpha-2 country code of the phone number. See:[ISO-3166](https://www.nationsonline.org/oneworld/country_code_list.htm).
            </ResponseField>

            <ResponseField name="lat_long" type="object">
              The coordinates of the geographical location of the address.
              <Expandable title="lat_long object">
                <ResponseField name="latitude" type="number">
                  The latitude coordinate of the location.
                </ResponseField>

                <ResponseField name="longitude" type="number">
                  The longer coordinate of the location.
                </ResponseField>

                <ResponseField name="accuracy" type="string">
                  The accuracy of the geographical location. Enum: "Neighborhood","Country","PostalCode","Street","City","State","RoofTop"
                </ResponseField>
              </Expandable>
            </ResponseField>

            <ResponseField name="is_active" type="boolean">
              True if the address is currently receiving mail.
            </ResponseField>

            <ResponseField name="delivery_point" type="string">
              The type of the delivery point of the address. Enum: "SingleUnit","MultiUnit","POBox","PartialAddress"
            </ResponseField>

            <ResponseField name="link_to_person_start_date" type="string">
              The date when the address was first linked to the person.
            </ResponseField>

            <ResponseField name="link_to_person_end_date" type="string">
              The date when the address was first linked to the person.
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="associated_people" type="object">
          The people that are associated to the person.
          <Expandable title="belongs_to object">
          <ResponseField name="id" type="string">
              The persistent ID of the address.
            </ResponseField>

            <ResponseField name="name" type="string">
              The full name of the person.
            </ResponseField>

            <ResponseField name="firstname" type="string">
              The first name of the person.
            </ResponseField>

            <ResponseField name="middlename" type="string">
              The middle name (or middle initial) of the person.
            </ResponseField>

            <ResponseField name="lastname" type="string">
              The last name of the person.
            </ResponseField>

          </Expandable>
        </ResponseField>

      </Expandable>
    </ResponseField>

    <ResponseField name="last_sale_date" type="string">
      The date of the last sale of the property. Available for US addresses only.
    </ResponseField>

    <ResponseField name="total_value" type="array">
      The total assessed tax value of the property in USD. Available for US
      addresses only.
    </ResponseField>

    <ResponseField name="owners" type="object">
      The list of the current owners of the property. Available for US addresses only.
      <Expandable title="owner object">
        <ResponseField name="id" type="string">
          The persistent ID of the address.
        </ResponseField>

        <ResponseField name="name" type="string">
          The full name of the person.
        </ResponseField>

        <ResponseField name="firstname" type="string">
          The first name of the person.
        </ResponseField>

        <ResponseField name="middlename" type="string">
          The middle name (or middle initial) of the person.
        </ResponseField>

        <ResponseField name="lastname" type="string">
          The last name of the person.
        </ResponseField>

        <ResponseField name="alternate_names" type="string">
          Alternate names of the person associated with the phone.
        </ResponseField>

        <ResponseField name="age_range" type="string">
          The age of the person in a 5-year range.
        </ResponseField>

        <ResponseField name="gender" type="string">
          The gender of the person.
        </ResponseField>

        <ResponseField name="type" type="string">
          The type of the legal entity.
            - Person: The legal entity is a person.
            - Company: The legal entity is a company.
        </ResponseField>

        <ResponseField name="link_to_phone_start_date" type="string">
          The date when the person was first linked to the phone number.
        </ResponseField>

        <ResponseField name="industry" type="array">
          Not applicable.
        </ResponseField>

      </Expandable>
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="error" type="object">
  Error details in case of an error.
  <Expandable title="error object">
    <ResponseField name="name" type="string">
      TIncomplete response due to external timeouts. **value:** "InternalError"
    </ResponseField>
    <ResponseField name="message" type="string">
      The error message. **value:** "Could not retrieve entire response"
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="warnings" type="array">
  Warnings returned as part of the response, if applicable. Possible values
  include: `Invalid Input`, `International number. Not authorized`, or `Missing
  Input`.
</ResponseField>

## Error Responses

### 400

- **Invalid Parameters Error**: Parameter called incorrectly.
- **Input Field Error**: Incorrect parameter information, such as non-existent area code on a phone number.
- **Input Error**: No parameters given.
- **Internal Error**: If there is an internal error while performing the checks

### 401

- **Bad Request**: If ';' used as parameter separator in request. Non-UTF-8-encoded request.
- **Not Authorized**: No api_key parameter in the request.

### 403

- **Auth Error**: API Key is not authorized for that request or expired.
- **Quota Exceeded Error**: API Key has exceeded its quota.

### 429 Too Many Requests

- API Key has exceeded its rate limit.

### 500 Internal Server Error

- An unexpected error occurred on the server.
